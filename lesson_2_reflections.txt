What happens when you initialize a repository? Why do you need to do it?

	It will initialize the git and we need to commit.

How is the staging area different from the working directory and the repository?
What value do you think it offers?

	Staging area is temp location before we commit. Working directory is location where work.  Repository is where we commit to.

How can you use the staging area to make sure you have one commit per logical
change?

	We add those files that related to 1 logical change, even though we have changed multiple files in the working directory.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

	When we want to have different version of the project. Branch will allow us to work with different project version and allow the programmer to focus implementing the feature on their branch.

How do the diagrams help you visualize the branch structure?
	
	By showing the commit hierarchies of a or between branch.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

	2 changes will be combined into 1. So we can understand which commits will be combined once we merge the branch.

What are the pros and cons of Gitâ€™s automatic merging vs. always doing merges
manually?

	Automatic will safe the time, but it will not know how to merge 2 lines which are changes in 2 different branches.